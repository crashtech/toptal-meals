{{!-- Record actions --}}
<nav class="btn-group" role="group">
  {{#link-to 'meals.record.edit' model.id class='btn btn-default'}}
    <i class="glyphicon glyphicon-pencil"></i>
    <span>Edit</span>
  {{/link-to}}
  {{#link-to 'meals.record.delete' model.id class='btn btn-default'}}
    <span>Delete</span>
    <i class="glyphicon glyphicon glyphicon-trash"></i>
  {{/link-to}}
</nav>

{{!-- Record data --}}
{{partial 'meals/record/infos'}}
